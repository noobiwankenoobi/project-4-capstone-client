// const store = require('./store.js')

// const loadGamesLists = (response) => {
//   const gamesListTemplate = require('./templates/gamesListView.handlebars')
//   const content = gamesListTemplate({ games: response.games })
//   $('#app').empty()
//   $('#app').html(content)
// }

module.exports = {
  // loadGamesLists
}
